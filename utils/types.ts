export type Log = {
    date: string
    user: string
    info: string
  }

export type githubStats = {
    commits: number;
    stars: number;
    issues: number;
    lastCommit: string;
    lastUpdated: Date;
}